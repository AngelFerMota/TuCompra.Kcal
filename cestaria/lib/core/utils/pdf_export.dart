/// Utilidades para exportar carritos/historial a PDF.
///
/// Paquetes comunes:
/// - `pdf` para generar documentos PDF en Flutter/Dart
/// - `printing` para previsualizar/compartir/imprimir
///
/// Recomendaciones:
/// - Diseñar una plantilla de documento (cabecera, listado de items, totales)
/// - Soportar localización (fechas, formatos numéricos)
/// - Incluir metadatos (autor, fecha, título)
///
/// No implementar lógica aquí; añade funciones más adelante (e.g. buildCartPdf).
class PdfExportUtils {
  // Ej.: Future<Uint8List> buildCartPdf(Cart cart);
}