/// Export utilities (PDF & CSV) for carts/history.
///
/// Suggested packages:
/// - pdf + printing (PDF generation/preview)
/// - csv (CSV build)
/// - share_plus (sharing)
/// - path_provider / file_saver (saving files)
///
/// Keep functions pure where possible. Actual IO can be abstracted.
class ExportUtils {
  // Example signatures to implement later:
  // Future<Uint8List> buildCartPdf(Cart cart);
  // Future<String> buildCartCsv(Cart cart);
}
