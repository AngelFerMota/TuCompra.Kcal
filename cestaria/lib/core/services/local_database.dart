/// Capa de persistencia local con SQLite (histórico, cachés, borradores).
///
/// Sugerencias técnicas:
/// - Paquete: `sqflite` (o `drift` si prefieres ORM en Dart)
/// - Entidades: productos cacheados, carritos archivados, items, preferencia de usuario
/// - Migraciones: versionado de esquema y upgrades seguros
/// - Transacciones para escritura de múltiples items del carrito
///
/// No implementar lógica aquí; deja contratos/DAOs para más adelante.
class LocalDatabase {
  // Define inicialización, DAOs y métodos CRUD cuando lo implementes.
}